/**
 * PROPERTY PREVIEW SLOT DEFAULT
 *
 * Returns null when no property modal is active.
 * Required to avoid 404 when the slot has no content.
 */
export default function PropertyPreviewDefault() {
  return null;
}
