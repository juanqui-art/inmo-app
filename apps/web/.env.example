# ============================================
# INMOAPP - ENVIRONMENT VARIABLES
# ============================================
#
# Copy this file to .env.local and fill in the values
# Never commit .env.local to version control!
#
# How to use:
# 1. cp .env.example .env.local
# 2. Fill in all required values
# 3. Restart the dev server: bun run dev
#
# ============================================

# ============================================
# SUPABASE CONFIGURATION (REQUIRED)
# ============================================
#
# Get these from: https://supabase.com/dashboard/project/_/settings/api
#
# NEXT_PUBLIC_* variables are exposed to the browser
# Keep the ANON key public-safe (it has RLS protection)

# Your Supabase project URL
# Example: https://abcdefghijklmnop.supabase.co
NEXT_PUBLIC_SUPABASE_URL=

# Your Supabase anon/public key (safe to expose to browser)
# Example: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# ============================================
# SUPABASE SERVICE ROLE KEY (OPTIONAL)
# ============================================
#
# DANGER: This key bypasses Row Level Security (RLS)
# Only use in server-side code (Server Actions, API Routes)
# Never expose to the browser!
#
# Get from: https://supabase.com/dashboard/project/_/settings/api
# Only needed for admin operations (bulk updates, bypassing RLS)

# SUPABASE_SERVICE_ROLE_KEY=

# ============================================
# DATABASE CONFIGURATION (REQUIRED)
# ============================================
#
# Get from: https://supabase.com/dashboard/project/_/settings/database
# Click "Connection String" → "URI" (not "Session pooler")
#
# Format: postgresql://postgres:[YOUR-PASSWORD]@[HOST]:5432/postgres
# Example: postgresql://postgres:mypass123@db.abcxyz.supabase.co:5432/postgres

DATABASE_URL=

# ============================================
# SITE CONFIGURATION (OPTIONAL)
# ============================================
#
# Your site's public URL (used for OG images, redirects, etc.)
# Defaults to http://localhost:3000 in development

# NEXT_PUBLIC_SITE_URL=https://tudominio.com

# ============================================
# DEVELOPMENT vs PRODUCTION
# ============================================
#
# NODE_ENV is automatically set by Next.js
# - "development" when running: bun run dev
# - "production" when running: bun run build && bun run start
# - "test" when running tests
#
# You don't need to set this manually!

# ============================================
# AI SEARCH CONFIGURATION (Session 2)
# ============================================
#
# OpenAI API Key for natural language search parsing
# Get from: https://platform.openai.com/api-keys
# Used for extracting location, price, features from user queries
#
OPENAI_API_KEY=

# ============================================
# UPSTASH REDIS - RATE LIMITING (OPTIONAL)
# ============================================
#
# Upstash provides serverless Redis for rate limiting.
# Without these, rate limiting is DISABLED (graceful degradation).
#
# Setup:
# 1. Create free account: https://upstash.com
# 2. Create Redis database (Regional, US-East-1)
# 3. Copy REST URL and Token from dashboard
#
# Free tier: 10,000 commands/day (enough for ~500 users)
#
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# ============================================
# SENTRY - ERROR MONITORING (OPTIONAL)
# ============================================
#
# Get from: https://sentry.io → Settings → Client Keys
#
# SENTRY_DSN=
# SENTRY_AUTH_TOKEN=

# ============================================
# EMAIL - RESEND (OPTIONAL)
# ============================================
#
# Get from: https://resend.com/api-keys
#
# RESEND_API_KEY=
# EMAIL_FROM_DOMAIN=             # Verified domain (e.g., "inmoapp.com") - leave empty for test mode

# ============================================
# FUTURE VARIABLES (Coming soon)
# ============================================
#
# GOOGLE_OAUTH_CLIENT_ID=        # Social login
# GOOGLE_OAUTH_CLIENT_SECRET=    # Social login

# ============================================
# TROUBLESHOOTING
# ============================================
#
# "Invalid environment variables" error?
# → Check that all REQUIRED variables are filled in
# → Restart dev server after changing .env.local
#
# "Database connection failed"?
# → Verify DATABASE_URL is correct
# → Check Supabase project isn't paused (free tier)
#
# "Authentication not working"?
# → Verify NEXT_PUBLIC_SUPABASE_URL and ANON_KEY match
# → Check RLS policies in Supabase dashboard
#
# Still stuck?
# → See TROUBLESHOOTING.md
# → Check Supabase logs: https://supabase.com/dashboard/project/_/logs
