{
  "name": "inmo-app",
  "version": "0.1.0",
  "private": true,
  "packageManager": "bun@1.2.23",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "db:gen": "cd packages/database && bunx prisma generate",
    "dev": "bun run db:gen && cd apps/web && bun run dev",
    "dev:web": "cd apps/web && bun run dev",
    "build": "bun run db:gen && bun run type-check && cd apps/web && bun run build",
    "start": "cd apps/web && bun run start",
    "lint": "bunx biome check .",
    "lint:fix": "bunx biome check . --write",
    "type-check": "cd apps/web && bunx tsc --noEmit",
    "format": "bunx biome format --write .",
    "test": "cd apps/web && bun run test || true",
    "test:web": "cd apps/web && bun run test",
    "pre-commit": "bun run type-check && bun run lint && bun run test",
    "ci": "bun run type-check && bun run lint && bun run test && bun run build",
    "postinstall": "bun run db:gen"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.5",
    "react-scan": "^0.4.3"
  },
  "dependencies": {
    "@radix-ui/react-toggle": "^1.1.10",
    "@radix-ui/react-toggle-group": "^1.1.11"
  }
}
